<%= simple_form_for [@restaurant, @review] do |f| %>
<%= f.input :rating %>
<%= f.input :content %>
<%= f.submit %>
<% end %>
<%= link_to "Retour a la page du restaurant", restaurant_path(@restaurant) %>
